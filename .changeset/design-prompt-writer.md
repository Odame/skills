---
"odame-skills": patch
---

Add the `design-prompt-writer` skill (productivity bucket, user-invoked, no upstream counterpart). It turns a brand, brief, audience, references, and creative standards into a foundation prompt for a new Claude Design project: an interview for facts only, since color, typeface, and layout are perceptual choices that Claude Design's own visual interview resolves better than a text description can.
