[project]
name = "odame-skills"
version = "0.1.0"
description = "uv workspace root for Python-based plugins in this repo"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = ["plugins/*"]

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8.0.0", "ruff>=0.8.0"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
